#include "main.h"
/**
 * _abs - calculate the absolue value of an integer
 * @t: The integer to be calculated
 * Return: The absolute value
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
